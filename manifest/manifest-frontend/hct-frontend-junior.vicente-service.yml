apiVersion: v1
kind: Service
metadata:
  name: junior-vicente-frontend-service
  namespace: junior-vicente-43-namespace
  labels:
    app: frontend-app
spec:
  selector:
    app: frontend-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
